name: Test

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

permissions:
  contents: read # to fetch code (actions/checkout)

defaults:
  run:
    shell: bash

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test-linux:
    strategy:
      matrix:
        python_version: ["3.8", "3.9", "3.10", "3.11", "3.12"]
        shell: ['bash', 'zsh', 'csh', 'tcsh', 'ksh', 'fish']
    runs-on: ubuntu-latest
    steps:
      - name: Install shell
        run : |
          apt install -y ${{ matrix.shell }}
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v4
        with:
          python-version: ${{ matrix.python_version }}
          allow-prereleases: true
      - name: Install
        run: |
          pip install ".[test]"
      - name: Make user
        run: |
          useradd -m mb110
          chsh -s $(which ${{ matrix.shell }}) mb110
      - name: Run in user
        run: |
          sudo -u mb110 echo $PATH
          sudo -u mb110 python -m puser
          sudo -u mb110 echo $PATH
